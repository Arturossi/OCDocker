# OCDocker Python Dependencies (Core Runtime)
#
# This file mirrors [project.dependencies] from pyproject.toml.
#
# Install core runtime dependencies:
#   pip install -r requirements.txt
#
# Install optional feature stacks:
#   pip install "ocdocker[docking]"
#   pip install "ocdocker[db]"
#   pip install "ocdocker[ml]"
#   pip install "ocdocker[analysis]"
#   pip install "ocdocker[workflow]"
#   pip install "ocdocker[all]"

configargparse>=1.7
joblib>=1.4.2
packaging>=25.0
pydantic>=2.11.4
pydantic-settings>=2.9.1
pyyaml>=6.0.2
requests>=2.32.3
rich>=13.9.1
tqdm>=4.66.5
