# OCDocker Python Dependencies (Core Runtime)
#
# This file mirrors [project.dependencies] in pyproject.toml exactly.
# pyproject.toml is the source of truth for version bounds and optional extras.
#
# Install core runtime dependencies:
#   pip install -r requirements.txt
#   pip install -e .
#
# Install optional feature stacks (defined in pyproject.toml):
#   pip install "ocdocker[docking]"
#   pip install "ocdocker[db]"
#   pip install "ocdocker[ml]"
#   pip install "ocdocker[analysis]"          # includes shap for OCScore export SHAP
#   pip install "ocdocker[workflow]"
#   pip install "ocdocker[all]"
#   pip install "ocdocker[full]"              # all runtime + docs build
#   pip install "ocdocker[dev]"               # pytest, ruff, mypy, pre-commit
#
# Typical developer install from a git checkout:
#   pip install -e ".[all,dev]"
#
# Packaged non-Python data (see [tool.setuptools.package-data] in pyproject.toml):
#   OCDocker.OCScore      -> Protocols/*.yml, Protocols/Ablations/*.yml
#   OCDocker.Workbench    -> static/*
#
# See docs/source/optional_dependencies.rst for the full extra reference.

configargparse>=1.7
joblib>=1.4.2
packaging>=25.0
pydantic>=2.11.4
pydantic-settings>=2.9.1
pyyaml>=6.0.2
requests>=2.32.3
rich>=13.9.1
tqdm>=4.66.5
